# Daily Report - 2026-04-25

## Contexto

Branch ativa: `submission-2`.

Objetivo da rodada: maximizar o score local da submissão C++20/uWebSockets/nginx para a Rinha de Backend 2026, mantendo somente ganhos concretos, sustentáveis e compatíveis com as restrições oficiais: porta 9999, rede bridge, 1 CPU total, 350 MB total, 1 load balancer sem lógica de aplicação e 2 APIs.

Baseline local recente antes da rodada:

| Configuração | p99 | final_score | FP | FN | HTTP errors | Decisão |
|---|---:|---:|---:|---:|---:|---|
| `api=0.45` cada, `nginx=0.10`; C++ baseline | 3.97ms | 5401.28 | 0 | 0 | 0 | referência |

## Experimentos

| Horário | Hipótese | Configuração / alteração | Resultado local | Decisão | Aprendizado |
|---|---|---|---|---|---|
| 13:40 | LTO/IPO no CMake pode melhorar hot path sem mudar lógica | `CMAKE_INTERPROCEDURAL_OPTIMIZATION_RELEASE ON` quando suportado | Run 1: p99 4.07ms, final 5390.70. Run 2: p99 4.02ms, final 5395.59. 0 FP/FN/HTTP. | Rejeitado e revertido | LTO não melhorou o binário neste workload; a diferença foi pequena, mas consistentemente abaixo do baseline. |
| 13:43 | Alinhar vetores e trocar `_mm256_loadu_ps` por `_mm256_load_ps` pode reduzir custo de leitura | Allocator alinhado para `dims_` e loads AVX alinhados | p99 4.54ms, final 5343.25. 0 FP/FN/HTTP. | Rejeitado e revertido | O ganho teórico de load alinhado não compensou o impacto no layout/alocação; `_mm256_loadu_ps` atual é melhor neste caso. |
| 13:44 | Reordenar ponteiros por requisição e remover `std::isfinite` do loop interno AVX2 pode reduzir instruções no hot path | Pré-ordenação local dos ponteiros de dimensão e branch de threshold fora do loop por dimensão | p99 5.17ms, final 5286.72. 0 FP/FN/HTTP. | Rejeitado e revertido | A tentativa manual piorou o código gerado/cache locality; a forma original com `dimension_order` direto permanece superior. |
| 13:58 | Transferir CPU do nginx para as APIs melhora KNN se o gargalo for CPU da API | `api=0.47` cada, `nginx=0.06` | p99 23.73ms, final 4624.63. 0 FP/FN/HTTP. | Rejeitado | O nginx em stream precisa de mais que 0.06 CPU no ramp local; subdimensionar o LB degrada fortemente a cauda. |
| 14:00 | Dar mais CPU ao nginx melhora a cauda se o gargalo for o proxy | `api=0.44` cada, `nginx=0.12` | Run 1: p99 3.76ms, final 5425.00. Run 2: p99 3.79ms, final 5421.48. 0 FP/FN/HTTP. | Aceito provisoriamente | O baseline estava mais sensível ao LB do que parecia; tirar 0.01 CPU de cada API e dar 0.02 ao nginx melhora p99. |
| 14:02 | Mais CPU ao nginx ainda pode melhorar sem matar as APIs | `api=0.43` cada, `nginx=0.14` | p99 3.76ms, final 5425.09. 0 FP/FN/HTTP. | Competitivo, não final | Resultado empatado com 0.44/0.12; sugere faixa ótima entre 0.42 e 0.44 por API. |
| 14:03 | Encontrar ponto ótimo do sweep de CPU | `api=0.42` cada, `nginx=0.16` | Run 1: p99 3.74ms, final 5426.84. Run 2: p99 3.76ms, final 5425.12. 0 FP/FN/HTTP. | Melhor configuração atual | Este ponto manteve detecção perfeita e ficou acima do baseline em duas medições, sem ultrapassar 1 CPU total. |
| 14:05 | CPU demais no nginx começa a prejudicar KNN | `api=0.41` cada, `nginx=0.18` | p99 3.85ms, final 5414.72. 0 FP/FN/HTTP. | Rejeitado | Passar de 0.16 CPU no nginx reduz CPU das APIs a ponto de piorar p99; `api=0.42/nginx=0.16` é o melhor ponto observado até agora. |
| 14:09 | Trocar nginx por HAProxy L4 sobre Unix sockets pode reduzir overhead do LB | `haproxy:3.1-alpine`, `mode tcp`, round-robin, mesmos UDS, CPU 0.16 | p99 3.79ms, final 5421.02. 0 FP/FN/HTTP. | Rejeitado e revertido | HAProxy é funcional e compatível com a regra de LB burro, mas não superou o nginx ajustado; a troca adiciona mudança operacional sem ganho claro. |
| 14:11 | Três APIs podem reduzir fila por instância mantendo 1 CPU total | `api1/api2/api3=0.28` CPU e 110MB cada; `nginx=0.16`; upstream com 3 UDS | p99 3.86ms, final 5413.81. 0 FP/FN/HTTP. | Rejeitado e revertido | A terceira instância reduz fila, mas a CPU por API cai demais para o KNN exato; 2 APIs maiores continuam melhores. |
| 14:16 | `-funroll-loops` pode ajudar o loop fixo de 14 dimensões | Adicionado ao binário e aos testes C++ | Build Docker subiu para cerca de 163s. Runtime: p99 4.71ms, final 5326.72. 0 FP/FN/HTTP. | Rejeitado e revertido | O compilador gerou código pior para este hot path; além de piorar runtime, a flag degrada muito a ergonomia de build. |
| 14:18 | TCP interno pode ser competitivo contra UDS com nginx | APIs escutando `0.0.0.0:3000`; upstream `api1:3000` e `api2:3000` | p99 4.11ms, final 5386.08. 0 FP/FN/HTTP. | Rejeitado e revertido | O overhead de TCP/bridge é mensurável; Unix sockets continuam melhores para nginx -> API. |
| 14:20 | Dois workers no nginx podem reduzir fila do LB | `worker_processes 2` com `nginx=0.16` | p99 3.99ms, final 5399.08. 0 FP/FN/HTTP. | Rejeitado e revertido | Com quota de 0.16 CPU para o LB, múltiplos workers aumentam contenção/context switch; 1 worker é melhor. |
| 14:22 | Remover `reuseport` pode reduzir overhead com 1 worker | `listen 9999 backlog=4096` sem `reuseport` | p99 4.04ms, final 5393.87. 0 FP/FN/HTTP. | Rejeitado e revertido | Mesmo com 1 worker, `reuseport` ajudou mais do que atrapalhou neste ambiente; manter. |
| 14:24 | `multi_accept off` pode suavizar lotes de accept | `multi_accept off` no nginx | p99 4.15ms, final 5381.80. 0 FP/FN/HTTP. | Rejeitado e revertido | Aceitar múltiplas conexões por evento é melhor neste ramp; desligar `multi_accept` aumenta cauda. |
| 14:28 | Reduzir cópias no parser com `std::string_view` para `known_merchants` e `merchant.id` | Parser manteve views temporárias sobre o `simdjson::padded_string`, sem alterar contrato nem vetor final | p99 5.24ms, final 5280.75. 0 FP/FN/HTTP. | Rejeitado e revertido | A redução de cópias pequenas no parser não atacou o gargalo real e piorou a cauda, possivelmente por layout/caminho de código menos favorável; o parser original com `std::string` é mais estável. |
| 14:30 | Evitar cópia do body HTTP quando o payload chega em chunk único | `main.cpp` passou a parsear diretamente o `std::string_view chunk` no caso `is_last && body.empty()` | Build local e Docker ok; `/ready` 204. k6: p99 4.95ms, final 5305.25. 0 FP/FN/HTTP. | Rejeitado e revertido | A cópia evitada não compensou a ramificação extra/caminho de código diferente no callback do uWS; o append único original é mais rápido neste ambiente. |
| 14:33 | Ajuste fino de CPU entre o ponto `0.42/0.16` e `0.43/0.14` pode encontrar máximo intermediário | `api=0.425` cada, `nginx=0.15`; limites efetivos inspecionados: 425M/425M/150M NanoCPUs | p99 4.06ms, final 5391.83. 0 FP/FN/HTTP. | Rejeitado | O ponto intermediário ficou pior que os dois vizinhos já medidos; o scheduler local parece favorecer quotas mais discretas em `api=0.42/nginx=0.16`. |
| 14:35 | Dar ainda mais CPU ao nginx pode melhorar a cauda além de `0.42/0.16` | `api=0.415` cada, `nginx=0.17`; limites efetivos: 415M/415M/170M NanoCPUs | p99 4.19ms, final 5378.21. 0 FP/FN/HTTP. | Rejeitado | Acima de 0.16 CPU no nginx a API perde CPU demais para o KNN exato; o ponto `api=0.42/nginx=0.16` continua sendo o melhor ajuste de quota. |
| 14:37 | Assumindo AVX2/FMA, remover `supports_avx2()` por requisição pode reduzir branch no hot path | `Classifier::classify` chamou `top5_avx2` diretamente em x86_64; build e testes C++ passaram; `/ready` 204 | p99 4.64ms, final 5333.03. 0 FP/FN/HTTP. | Rejeitado e revertido | O runtime check não era gargalo relevante; a forma original gera caminho mais estável, e a remoção ainda piora portabilidade sem ganho. |
| 14:39 | Remover `Content-Type: application/json` da resposta 200 reduz bytes e chamadas no hot path | `main.cpp` respondeu apenas body JSON e `Content-Length`; sample manual continuou 200 com JSON parseável | p99 5.17ms, final 5286.58. 0 FP/FN/HTTP. | Rejeitado e revertido | Além de aumentar ambiguidade contratual, a remoção piorou fortemente a cauda; manter o header explícito é melhor e mais correto. |
| 14:41 | Usar `res->cork` no envio pode reduzir syscalls/pacotes no callback `onData` | `writeHeader` e `end` foram agrupados em `res->cork([res, body] { ... })`; build e `/ready` ok | p99 5.20ms, final 5283.88. 0 FP/FN/HTTP. | Rejeitado e revertido | Cork manual no uWS degradou a cauda; o caminho direto `writeHeader` + `end` é superior para esta topologia com nginx stream. |
| 14:42 | Reduzir período CFS pode cortar pausas de throttling mantendo a mesma CPU total | Adicionados `cpu_period: 10000` e `cpu_quota` proporcional aos serviços, mantendo `deploy.resources.limits.cpus` | `docker compose up` falhou: `Conflicting options: Nano CPUs and CPU Period cannot both be set`. | Rejeitado e revertido | Não é sustentável remover `deploy.resources.limits.cpus`, porque isso enfraquece a conformidade e a validação oficial de recursos; manter `cpus` declarativo. |
| 14:44 | Reduzir buffer do nginx stream para payloads pequenos pode melhorar cache/memória por conexão | `proxy_buffer_size 1k` no server stream; `nginx -t` ok; `/ready` 204 | p99 4.27ms, final 5369.24. 0 FP/FN/HTTP. | Rejeitado e revertido | O buffer padrão do nginx é mais eficiente neste perfil; reduzir para 1k aumenta cauda e não traz benefício observável. |
| 14:46 | Tuning de allocator glibc pode reduzir custo de alocações pequenas por requisição | APIs com `MALLOC_ARENA_MAX=1` e `MALLOC_TRIM_THRESHOLD_=-1`; env validado em `api1` e `api2`; `/ready` 204 | p99 4.10ms, final 5387.67. 0 FP/FN/HTTP. | Rejeitado e revertido | O padrão do glibc é melhor para o processo single-thread atual; restringir arena/trim não reduziu cauda. |
| 14:48 | Reduzir frequência do prune AVX2 pode diminuir branches no loop de KNN | `top5_avx2` passou a checar o threshold a cada 2 dimensões acumuladas em vez de a cada dimensão | p99 4.55ms, final 5341.61. 0 FP/FN/HTTP. | Rejeitado e revertido | O custo de acumular mais dimensões antes do corte supera a economia de branches; o prune por dimensão continua melhor para este layout ordenado por variância. |
| 14:51 | Reaproveitar buffers temporários do parser reduz alocações por request | `parse_payload` usou scratch `thread_local` para `known_merchants` e `merchant_id`; testes C++ passaram; `/ready` 204 após rebuild | p99 5.35ms, final 5272.03. 0 FP/FN/HTTP. | Rejeitado e revertido | O custo de TLS/layout e o caminho de código alterado superaram qualquer economia de alocação; o parser original com temporários locais é mais rápido e estável. |
| 14:53 | Parser fixo de timestamp pode reduzir custo de vetorização | `parse_timestamp` trocou loop genérico de dígitos por leitura direta de 2/4 dígitos, preservando validação de formato/calendário; testes C++ passaram; `/ready` 204 | p99 4.99ms, final 5302.32. 0 FP/FN/HTTP. | Rejeitado e revertido | A micro-otimização piorou o binário/caminho de branch no stack completo; o parser de dígitos em loop curto é mais competitivo. |
| 14:56 | Usar buffer interno reutilizável do simdjson evita `padded_string` por request | `parse_payload` chamou `parser.parse(body.data(), body.size())`, mantendo padding seguro via cópia interna do parser; testes C++ passaram; `/ready` 204 | p99 5.28ms, final 5277.52. 0 FP/FN/HTTP. | Rejeitado e revertido | O caminho `padded_string` local continua mais rápido neste workload; a cópia interna/reuso do parser alterou o hot path e aumentou cauda. |
| 14:59 | Converter `merchant.mcc` diretamente para risco numérico no parse elimina string no vetor | `Payload` passou temporariamente a armazenar `merchant_mcc_risk`; parser calculou risco por `std::string_view`; testes C++ passaram; `/ready` 204 | p99 5.10ms, final 5292.44. 0 FP/FN/HTTP. | Rejeitado e revertido | A cópia de MCC e a função `mcc_risk` fora do parser não são gargalos; alterar o layout do `Payload` e antecipar o cálculo piorou a cauda. |
| 15:02 | Tirar `std::isfinite(threshold)` de dentro do loop AVX2 reduz branch por dimensão | `top5_avx2` separou o primeiro chunk sem threshold finito do caminho com prune, sem mudar ordem de dimensão nem ponteiros; testes C++ passaram; `/ready` 204 | p99 4.70ms, final 5328.24. 0 FP/FN/HTTP. | Rejeitado e revertido | A duplicação do loop piorou instruções/cache de instrução; o branch original dentro do loop é melhor para este compilador e dataset. |
| 15:04 | `deferred` no `listen` do nginx stream pode reduzir wakeups em conexões novas | `listen 9999 reuseport backlog=4096 deferred`; `nginx -t` ok; rebuild completo para evitar imagem antiga; `/ready` 204 | p99 4.07ms, final 5390.78. 0 FP/FN/HTTP. | Rejeitado e revertido | `TCP_DEFER_ACCEPT` não ajudou este padrão de k6/keep-alive; o listen original sem `deferred` mantém menor cauda. |
| 15:06 | Backlog maior no nginx pode reduzir fila de accept sob ramp | `listen 9999 reuseport backlog=8192`; `nginx -t` ok; APIs mantidas com imagem limpa; `/ready` 204 | p99 4.04ms, final 5394.09. 0 FP/FN/HTTP. | Rejeitado e revertido | A fila de accept não parece ser gargalo; `backlog=4096` continua melhor ou equivalente com menor superfície de tuning. |
| 15:08 | Mais conexões por worker no nginx pode evitar limite interno sob ramp | `worker_connections 8192`; `nginx -t` ok; `/ready` 204 | p99 3.87ms, final 5411.77. 0 FP/FN/HTTP. | Rejeitado e revertido | Foi o melhor ajuste nginx recente, mas ainda abaixo do melhor aceito; `4096` permanece a configuração final por evidência. |
| 15:10 | Menos conexões por worker reduz overhead interno do nginx | `worker_connections 2048`; `nginx -t` ok; `/ready` 204 | p99 4.01ms, final 5396.67. 0 FP/FN/HTTP. | Rejeitado e revertido | Reduzir a capacidade não melhora a cauda; o valor `4096` segue como melhor compromisso medido. |
| 15:11 | Mais memória para nginx pode evitar pressão interna do proxy | APIs `160MB` cada, nginx `30MB`, CPUs mantidas em `0.42/0.42/0.16`; limites efetivos inspecionados | p99 4.05ms, final 5392.80. 0 FP/FN/HTTP. | Rejeitado e revertido | A cauda não é limitada por memória do nginx; manter `165MB` por API e `20MB` no LB é melhor. |
| 15:13 | Evitar refcount de `shared_ptr` na captura do callback reduz overhead por request | `run_server` capturou `AppState*` no `post/onData` mantendo lifetime pelo `shared_ptr` externo; testes C++ passaram; `/ready` 204 | p99 4.56ms, final 5341.46. 0 FP/FN/HTTP. | Rejeitado e revertido | A forma original com `shared_ptr` é mais estável; o refcount não é gargalo mensurável e a troca reduz segurança sem ganho. |
| 15:16 | Buffer inline de 1KB para body evita alocação de `std::string` por request | `RequestContext` usou `std::array<char,1024>` com fallback para `std::string`; testes C++ passaram; `/ready` 204 | p99 4.65ms, final 5332.96. 0 FP/FN/HTTP. | Rejeitado e revertido | Aumentar o `RequestContext` e copiar manualmente piorou a cauda; a `std::string` original é mais eficiente neste caminho do uWS. |
| 15:20 | Inline storage para `customer.known_merchants` evita alocação do vetor pequeno | `KnownMerchants` local com `std::array<std::string,16>` e overflow vector; testes C++ passaram; rebuild completo; `/ready` 204 | p99 4.94ms, final 5306.46. 0 FP/FN/HTTP. | Rejeitado e revertido | O caminho original com `std::vector<std::string>` local é melhor; o storage inline aumentou objeto/código e piorou a cauda apesar de evitar alocações em casos pequenos. |
| 15:22 | Compilar `uSockets` e `simdjson` com `x86-64-v3` pode acelerar parser/servidor | Adicionados `-march=x86-64-v3` aos targets estáticos `usockets` e `simdjson_singleheader`; testes C++ passaram; rebuild completo; `/ready` 204 | p99 6.03ms, final 5219.98. 0 FP/FN/HTTP. | Rejeitado e revertido | Forçar target nas libs piorou muito a cauda; o build Release genérico de `uSockets`/`simdjson` é mais estável, e o AVX2 deve permanecer restrito ao binário/hot path atual. |
| 15:26 | Repetir o baseline aceito separa ganho real de ruído local | Estado limpo com `api=0.42` cada e `nginx=0.16`; rebuild completo depois de reverter a build rejeitada; 3x k6 sequencial | Run 1: p99 4.06ms, final 5391.75. Run 2: p99 4.04ms, final 5394.06. Run 3: p99 4.06ms, final 5391.51. 0 FP/FN/HTTP em todos. | Reabrir comparação | O melhor histórico de 3.74ms não se reproduziu nesta janela; antes de manter o ajuste de CPU como definitivo, é necessário revalidar a configuração anterior no mesmo estado da máquina. |
| 15:30 | Revalidar a configuração anterior no mesmo estado da máquina | `api=0.45` cada, `nginx=0.10`; recriação dos containers; limites efetivos 450M/450M/100M NanoCPUs | Run 1: p99 4.05ms, final 5392.99. Run 2: p99 4.07ms, final 5390.75. Run 3: p99 3.98ms, final 5400.51. 0 FP/FN/HTTP em todos. | Competitivo, comparação reaberta | A configuração original ficou tão boa ou melhor que `0.42/0.16` na janela atual; a escolha final deve considerar média/estabilidade recente, não o melhor resultado histórico isolado. |
| 15:34 | Revalidar ponto intermediário de CPU que havia melhorado antes | `api=0.44` cada, `nginx=0.12`; recriação dos containers; limites efetivos 440M/440M/120M NanoCPUs | Run 1: p99 4.01ms, final 5396.98. Run 2: p99 4.03ms, final 5394.95. Run 3: p99 3.99ms, final 5398.69. 0 FP/FN/HTTP em todos. | Candidato final recente | Esse ponto foi o mais estável da reamostragem recente até agora; a média ficou melhor que `0.42/0.16` e ligeiramente melhor que `0.45/0.10`. |
| 15:37 | Revalidar ponto `0.43/0.14` sob a mesma janela | `api=0.43` cada, `nginx=0.14`; recriação dos containers; limites efetivos 430M/430M/140M NanoCPUs | Run 1: p99 4.02ms, final 5395.82. Run 2: p99 3.95ms, final 5403.34. Run 3: p99 4.12ms, final 5384.74. 0 FP/FN/HTTP em todos. | Rejeitado como final | Apesar de um bom run isolado, a variância foi maior e a média ficou pior que `0.44/0.12`; não é o melhor ponto sustentável. |
| 15:41 | Ajuste fino entre `0.44/0.12` e `0.45/0.10` pode equilibrar API/LB | `api=0.445` cada, `nginx=0.11`; recriação dos containers; limites efetivos 445M/445M/110M NanoCPUs | Run 1: p99 3.88ms, final 5410.73. Run 2: p99 4.08ms, final 5388.84. Run 3: p99 3.98ms, final 5399.61. 0 FP/FN/HTTP em todos. | Candidato final, requer cautela | Teve a melhor média recente e o melhor run da janela, mas ainda com variância; é melhor que `0.44/0.12` por evidência local, porém o ganho é pequeno e não deve ser vendido como ganho algorítmico. |
| 15:45 | Fino ainda mais próximo do original pode preservar API e aliviar pouco o LB | `api=0.4475` cada, `nginx=0.105`; recriação dos containers; limites efetivos 447.5M/447.5M/105M NanoCPUs | Run 1: p99 3.95ms, final 5403.57. Run 2: p99 4.09ms, final 5388.68. Run 3: p99 4.11ms, final 5386.55. 0 FP/FN/HTTP em todos. | Rejeitado | O LB com 0.105 CPU voltou a apresentar cauda pior; `0.445/0.11` permanece o melhor ponto fino recente. |
| 15:50 | `mimalloc` pode reduzir overhead de alocação pequena por request | Runtime instalou `libmimalloc2.0` e APIs usaram `LD_PRELOAD=/usr/lib/x86_64-linux-gnu/libmimalloc.so.2`; CPU `0.445/0.11`; rebuild sem cache; `/ready` 204 | Run 1: p99 4.69ms, final 5328.68. Run 2: p99 4.01ms, final 5397.28. Run 3: p99 4.05ms, final 5392.83. 0 FP/FN/HTTP em todos. | Rejeitado e revertido | O allocator alternativo aumentou variância e piorou a média; glibc padrão continua melhor para esta API single-thread com poucas alocações críticas. |
| 15:55 | `jemalloc` pode ser mais estável que `mimalloc` nas alocações do uWS/simdjson | Runtime instalou `libjemalloc2` e APIs usaram `LD_PRELOAD=/usr/lib/x86_64-linux-gnu/libjemalloc.so.2`; CPU `0.445/0.11`; rebuild sem cache; `/ready` 204 | Run 1: p99 4.70ms, final 5328.21. Run 2: p99 4.11ms, final 5385.86. Run 3: p99 4.01ms, final 5397.16. 0 FP/FN/HTTP em todos. | Rejeitado e revertido | `jemalloc` também piorou variância/média; encerrar a linha de allocators e manter glibc padrão. |
| 16:02 | Nginx mainline mais novo pode melhorar stream/proxy | `nginx:1.29-alpine` (`nginx/1.29.8`), mantendo `api=0.445` cada e `nginx=0.11`; API rebuild limpa sem allocator alternativo; `/ready` 204 | Run 1: p99 4.17ms, final 5380.06. Run 2: p99 4.06ms, final 5391.92. Run 3: p99 4.18ms, final 5378.62. 0 FP/FN/HTTP em todos. | Rejeitado | A versão mais nova piorou a cauda; a tag `1.27-alpine` usada no baseline continua mais competitiva até prova contrária. |
| 16:06 | Nginx estável intermediário pode ser melhor que 1.29 e 1.27 | `nginx:1.28-alpine` (`nginx/1.28.3`), mantendo `api=0.445` cada e `nginx=0.11`; `/ready` 204 | Run 1: p99 4.09ms, final 5388.42. Run 2: p99 4.20ms, final 5376.64. Run 3: p99 4.03ms, final 5394.52. 0 FP/FN/HTTP em todos. | Rejeitado e revertido | Também piorou a média e teve cauda instável; manter `nginx:1.27-alpine`. |
| 16:11 | Remover `shared_ptr` do `RequestContext` reduz refcount por request | `RequestContext` foi alocado com `new`, deletado no `onAborted` ou por `unique_ptr` no `is_last`; `nginx:1.27-alpine`; CPU `0.445/0.11`; rebuild completo; `/ready` 204 | Run 1: p99 5.00ms, final 5300.78. Run 2: p99 4.28ms, final 5368.85. Run 3: p99 4.05ms, final 5392.51. 0 FP/FN/HTTP em todos. | Rejeitado e revertido | O `shared_ptr` não é o gargalo dominante e o cleanup manual piorou a cauda; manter a versão segura/original. |
| 16:15 | Remover header automático `uWebSockets: 20` reduz bytes por resposta | Adicionado `UWS_HTTPRESPONSE_NO_WRITEMARK`; resposta manual manteve `Content-Type`, `Date` e `Content-Length`, sem `uWebSockets`; CPU `0.445/0.11`; rebuild completo | Run 1: p99 4.63ms, final 5334.80. Run 2: p99 4.10ms, final 5387.01. Run 3: p99 4.09ms, final 5388.64. 0 FP/FN/HTTP em todos. | Rejeitado e revertido | Menos bytes não significou menor cauda; o caminho padrão do uWS com mark é mais estável neste build. |
| 16:20 | Guardar bucket `0..5` em `Classification` evita `floor/clamp` no response path | `Classification` ganhou `fraud_bucket`, `classifier` preencheu o bucket e `classification_json` deixou de recalcular; testes C++ passaram; rebuild completo; `/ready` 204 | Run 1: p99 4.75ms, final 5323.45. Run 2: p99 4.20ms, final 5376.72. Run 3: p99 4.01ms, final 5396.98. 0 FP/FN/HTTP em todos. | Rejeitado e revertido | A economia de float/clamp é irrelevante e a mudança de layout/código piorou a cauda; manter `Classification` enxuta. |
| 16:21 | Priorizar worker do nginx pode reduzir cauda do LB | `worker_priority -5` no `nginx.conf`; configuração validou sintaxe, mas logou `setpriority(-5) failed (13: Permission denied)` | Não medido por não aplicar de fato. | Rejeitado e revertido | Sem `privileged`/capacidade extra, o ajuste não é operacionalmente válido; não vale introduzir ruído nem dependência de permissões. |
| 16:25 | `tcp_nodelay on` no nginx stream pode reduzir atraso em respostas pequenas | Adicionado `tcp_nodelay on` no `server` stream; `nginx -t` ok; CPU `0.445/0.11`; `/ready` 204 | Run 1: p99 4.05ms, final 5392.73. Run 2: p99 4.18ms, final 5378.54. Run 3: p99 4.05ms, final 5392.60. 0 FP/FN/HTTP em todos. | Rejeitado e revertido | O ajuste não reduziu p99 e aumentou a cauda em um dos runs; manter o padrão do nginx stream. |
| 16:29 | Pré-computar ponteiros das dimensões reduz trabalho por request no AVX2 | `ReferenceSet` armazenou `dim_data_` após `shrink_to_fit`, e `top5_avx2` deixou de montar o array local de ponteiros; testes C++ passaram; rebuild completo; `/ready` 204 | Run 1: p99 4.79ms, final 5319.48. Run 2: p99 4.11ms, final 5385.65. Run 3: p99 4.01ms, final 5396.32. 0 FP/FN/HTTP em todos. | Rejeitado e revertido | A economia de 14 chamadas `data()` por request é irrelevante; o layout maior de `ReferenceSet`/código gerado piorou a cauda. |
| 16:33 | `-O2` pode reduzir pressão de instruções frente ao `-O3` padrão | Adicionado `-O2` apenas a `rinha-backend-2026-cpp` e aos testes, preservando libs no Release padrão; testes C++ passaram; rebuild completo; `/ready` 204 | Run 1: p99 4.95ms, final 5305.22. Run 2: p99 4.28ms, final 5368.50. Run 3: p99 4.03ms, final 5394.89. 0 FP/FN/HTTP em todos. | Rejeitado e revertido | O hot path AVX2/KNN se beneficia mais do `-O3` padrão do Release; `-O2` piora a cauda. |
| 16:38 | Backlog do Unix socket da API pode reduzir fila nginx -> API | Patch temporário em uSockets: `listen(..., 512)` para `listen(..., 4096)` no socket Unix; testes C++ passaram; rebuild completo; `/ready` 204 | Run 1: p99 4.94ms, final 5306.26. Run 2: p99 3.99ms, final 5398.92. Run 3 inicial falhou por DNS do k6 ao baixar `jslib.k6.io`; reexecução: p99 3.99ms, final 5399.50. 0 FP/FN/HTTP nos runs válidos. | Rejeitado e revertido | Não superou claramente o candidato atual e exigia patch em código vendored; o backlog padrão 512 do uSockets não é o gargalo principal nesta carga. |
| 16:42 | Ponto fino `0.4425/0.115` pode combinar estabilidade de `0.44/0.12` e média de `0.445/0.11` | `api=0.4425` cada, `nginx=0.115`; limites efetivos 442.5M/442.5M/115M NanoCPUs; `/ready` 204 | Run 1: p99 4.39ms, final 5357.23. Run 2: p99 4.14ms, final 5382.54. Run 3: p99 3.97ms, final 5400.85. 0 FP/FN/HTTP em todos. | Rejeitado | Média e cauda piores que `0.445/0.11`; o ajuste fino não justifica troca. |
| 16:48 | Validar candidato `0.445/0.11` em amostra maior antes de finalizar | `api=0.445` cada, `nginx=0.11`; rebuild limpo e recriação completa; limites efetivos 445M/445M/110M NanoCPUs | Run 1: p99 4.13ms, final 5383.90. Run 2: p99 3.97ms, final 5401.15. Run 3: p99 4.25ms, final 5371.30. Run 4: p99 4.20ms, final 5376.60. Run 5: p99 4.08ms, final 5389.64. 0 FP/FN/HTTP em todos. | Rejeitado como final | A amostra maior não sustentou o bom run de 3.88ms; a média ficou pior que `0.44/0.12`. |
| 16:54 | Validar `0.44/0.12` como configuração final da rodada | `api=0.44` cada, `nginx=0.12`; recriação completa; limites efetivos 440M/440M/120M NanoCPUs | Run 1: p99 4.08ms, final 5389.56. Run 2: p99 3.92ms, final 5407.21. Run 3: p99 4.08ms, final 5389.71. Run 4: p99 4.08ms, final 5388.91. Run 5: p99 4.04ms, final 5393.53. 0 FP/FN/HTTP em todos. | Aceito como final da rodada | É o melhor compromisso recente: não repete o melhor histórico isolado de 3.74ms, mas teve média/estabilidade melhores que `0.42/0.16`, `0.445/0.11` e pontos adjacentes na reamostragem. |

## Estado Atual Aceito

Configuração final mantida no `docker-compose.yml`:

| Serviço | CPU | Memória |
|---|---:|---:|
| `api1` | 0.44 | 165MB |
| `api2` | 0.44 | 165MB |
| `nginx` | 0.12 | 20MB |
| Total | 1.00 | 350MB |

Melhor resultado observado da configuração final na validação de encerramento:

```json
{
  "p99": "3.92ms",
  "final_score": 5407.21,
  "false_positive_detections": 0,
  "false_negative_detections": 0,
  "http_errors": 0
}
```

Observação: o melhor run histórico da tarde foi `p99 3.74ms / final_score 5426.84` com `api=0.42/nginx=0.16`, mas a reamostragem 3x posterior desse mesmo ponto retornou `4.06ms`, `4.04ms`, `4.06ms`. Por isso ele foi tratado como outlier local e não como configuração final.

## Próximas Hipóteses

1. Não insistir em micro-otimizações isoladas de parser/callback/headers sem profiler: todas as tentativas pequenas pioraram p99.
2. Se for abrir nova frente, ela precisa ser estrutural e mensurável: parser especializado completo, outro servidor HTTP/L4 com baseline próprio, ou mudança de algoritmo com prova de erro zero.
3. Para submissão local imediata, manter `0.44/0.12`, nginx 1.27, UDS, glibc padrão e KNN exato AVX2 atual.
